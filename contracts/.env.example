# Copy to .env and fill in. NEVER commit .env.
# Deployer private key (a throwaway testnet key funded from https://faucet.monad.xyz)
PRIVATE_KEY=0x...

# Monad testnet (chain id 10143)
MONAD_TESTNET_RPC=https://testnet-rpc.monad.xyz

# Explorer verification (Monad testnet uses a Blockscout-style verifier;
# see the "How to verify a smart contract" guide in the Monad docs).
MONAD_EXPLORER_URL=https://testnet.monadexplorer.com
